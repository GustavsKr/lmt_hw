to clarify:

- jet 1000 eur/minute? do i need to calculate how many minutes the jet would spend hitting the target based on the target speed? same with 50cal 1 eur per shot - do i assume that it takes only 1 shot to shoot the target down?
- the radar data every second is coming from an external api or should i make simulated POST requests every second?
- strike the target only based on the threat classification level?